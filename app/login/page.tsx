import Login from '@/features/login';

export default function LoginPage() {
  return <Login />;
}

export const metadata = {
  title: 'Login - Dashboard Task',
  description: 'Sign in to your account',
};
